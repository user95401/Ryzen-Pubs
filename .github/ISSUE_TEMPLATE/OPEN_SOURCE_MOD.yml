name: "Open Source Mod (.geode or .dll on github repo with releases)"
description: "Ryzen will get all info from repository. Download link is taken from releases."

labels: "mod"
title: "Unnamed Open Source Mod"

body:

- type: textarea
  attributes:
    label: "Main Section"
    value: |
      [main]
      
      ; Description about your mod in 1-2 text lines 
      desc = Just cool mod, trust me
      
      ; If mod not yours or whatever (not important key)
      developer = 
      
      ; Custom link to logo image  (not important key)
      logo_url = 
      
      ; not important key
      github_page_link = 
      
      ; not important key
      website_link = 
      
      ; not important key
      custom_bg_link = 
      
      ; not important key
      overlay_link = 
  validations:
    required: true
    
- type: textarea
  attributes:
    label: "Repository Section"
    value: |
      [repo]
      ; https://github.com/{repo.owner}/{repo.name}
      
      owner = TheUltimateModMaker
      
      name = the_mod
  validations:
    required: true
    
- type: textarea
  attributes:
    label: "Release Section"
    value: |
      [release]
      
      ; also can be id or "tag/{your_tag}"
      tar = latest
      
      file = theultimatemodmaker.the-mod
  validations:
    required: true
